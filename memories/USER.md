Projeto no GitHub (Pedro0701/Conversor). Commits automáticos em português com descrições naturais e emoji. Telegram configurado (bot + chat ID). Vault Obsidian em ~/Documents/Obsidian Vault/.
§
ALL backups must end up in SQL Server regardless of source SGBD. Pipeline: native restore → _migrar_*_para_sqlserver() → NVARCHAR(MAX) via _shared. Git commits: português com emoji.
§
Pedro (Pedro0701/Conversor). BR, PT-BR. Direto, prático, token-efficient (kanban+watchdog). Quer ALL backups convertidos pra SQL Server. Gosta de organização limpa e portabilidade (VM→VPS). Aprovou MCP + aprendizado contínuo.
§
Pedro (Pedro0701/Conversor): direto, prático, português BR. Prefere diagnóstico antes de proposta de correção — "identifique os erros e monte um relatorio" antes de aplicar mudanças. Quer relatórios organizados com tabelas, status indicators (✅/❌), e root cause analysis clara. Valoriza autonomia do pipeline (watchdog → kanban → worker deve funcionar sem intervenção). Corrigiu meu approach diversas vezes — não quer que eu aplique correções sem antes mostrar o diagnóstico completo.